Create secrets with following commands before get start with install.

kubectl create namespace datadog
kubectl create secret generic datadog-secret -n datadog --from-literal api-key=$DD_API_KEY --from-literal app-key=$DD_APP_KEY
